version: 0.1

clowder_configs:
  - name: cats
    clowder_dir: /Users/jrgoodle/projects/cats
    defaults:
      jobs: 4
      projects: wrong_type # ERROR
      # projects: [kit, kishka]
      protocol: https

