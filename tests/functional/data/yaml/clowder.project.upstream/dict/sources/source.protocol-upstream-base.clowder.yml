name: source.protocol-upstream-base

protocol: https

sources:
  chromium:
    url: chromium.googlesource.com

clowder:
  - name: JrGoodle/gyp
    upstream:
      name: external/gyp
      source: chromium
