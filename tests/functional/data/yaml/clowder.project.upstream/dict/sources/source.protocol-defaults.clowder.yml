name: source.protocol-defaults

sources:
  chromium:
    url: chromium.googlesource.com
    protocol: https

defaults:
  upstream:
    source: chromium

clowder:
  - name: JrGoodle/gyp
    upstream:
      name: external/gyp
