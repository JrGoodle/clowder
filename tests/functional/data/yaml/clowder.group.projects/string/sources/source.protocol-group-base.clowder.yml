name: source.protocol-group-base

protocol: https

sources:
  beanstalk:
    url: polka-dot-cat.git.beanstalkapp.com

clowder:
  cats:
    defaults:
      source: beanstalk
    projects:
      - felidae
