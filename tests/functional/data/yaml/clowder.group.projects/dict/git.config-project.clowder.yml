name: git.config-project

clowder:
  cats:
    projects:
      - name: JrGoodle/felidae
        git:
          config:
            alias.something: "!echo \"Run 'git something' alias installed via clowder\"; touch something"
