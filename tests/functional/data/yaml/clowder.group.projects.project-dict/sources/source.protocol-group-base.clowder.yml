name: source.protocol-group-base

protocol: https

sources:
  chromium:
    url: chromium.googlesource.com

clowder:
  cats:
    defaults:
      source: chromium
    projects:
      - name: external/gyp
