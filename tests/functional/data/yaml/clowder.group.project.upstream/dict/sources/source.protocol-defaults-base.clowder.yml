name: source.protocol-defaults-base

protocol: https

sources:
  chromium:
    url: chromium.googlesource.com

defaults:
  upstream:
    source: chromium

clowder:
  cats:
    - name: JrGoodle/gyp
      upstream:
        name: external/gyp
