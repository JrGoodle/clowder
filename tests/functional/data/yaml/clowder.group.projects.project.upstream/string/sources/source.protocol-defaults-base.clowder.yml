name: source.protocol-defaults-base

protocol: https

sources:
  chromium:
    url: chromium.googlesource.com

defaults:
  upstream:
    source: chromium

clowder:
  cats:
    projects:
      - name: JrGoodle/gyp
        upstream: external/gyp
