name: source.url-defaults

sources:
  beanstalk:
    url: polka-dot-cat.git.beanstalkapp.com

clowder:
  cats:
    defaults:
      upstream:
        source: beanstalk
    projects:
      - name: JrGoodle/felidae
        upstream: polka-dot-cat/felidae
