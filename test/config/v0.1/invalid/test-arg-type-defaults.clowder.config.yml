version: 0.1

clowder_configs:
  - name: cats
    clowder_dir: /Users/jrgoodle/projects/cats
    defaults: [wrong_type] # ERROR
      # parallel: true
      # projects: [kit, kishka]
      # protocol: https

